import { Typography } from "@material-ui/core";
import React, { Component } from "react";

export default class WaktuGenap extends Component {
  render() {
    return (
      <div>
        <Typography variant="h2">Genap</Typography>
      </div>
    );
  }
}
